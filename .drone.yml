# Kanopy CI/CD for multi-agent-retail-advisor-with-memory.
#
# Our app has 5 services (frontend, orchestrator, planner, profile, product) but the
# mongodb/web-app chart is built around "one release = one pod". We split them as:
#   - "app" release:      frontend (main container) + orchestrator (sidecar, loopback-only)
#   - "planner" release:  planner-agent-service, standalone pod/Service/ingress
#   - "profile" release:  profile-agent-service, standalone pod/Service/ingress
#   - "product" release:  product-agent-service, standalone pod/Service/ingress
#
# planner/profile/product each get their own Kubernetes Service + ingress so they
# stay independently reachable/discoverable over the network (real A2A, not sealed
# inside one pod) — see CLAUDE.md's "every agent hop is a real network call".
#
# Both the backend image (built once, reused 3x via the SERVICE env var — see
# backend/docker-entrypoint.sh) and the frontend image are built from their existing
# subdirectory Dockerfiles (no need to relocate them to the repo root).
#
# Port convention: every container listens on 8080 (the chart's own default) so we
# never need to override `services[]`/`sidecarContainers[...].ports` at all — this
# matches every other demo release sampled in this cluster (capitalmarkets-*,
# leafy-bank-*, insurance-agentic-*, etc. all just listen on 8080 and rely on chart
# defaults). PLANNER_AGENT_PORT/PROFILE_AGENT_PORT/PRODUCT_AGENT_PORT are set to 8080
# in environment/*.yaml for Kanopy specifically; local/docker-compose keeps its own
# ports (8081/9091/9092) unchanged.

---
kind: pipeline
type: kubernetes
name: staging

trigger:
  branch:
    - staging

steps:
  - name: publish-backend
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/multi-agent-retail-advisor-backend
      dockerfile: backend/Dockerfile
      context: backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  - name: publish-frontend
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/multi-agent-retail-advisor-frontend
      dockerfile: frontend/Dockerfile
      context: frontend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # frontend (main) + orchestrator (sidecar, reached by frontend via localhost:8080)
  - name: deploy-app-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor
      values_files: ["environment/staging-app.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-frontend"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor.industrysolutions.staging.corp.mongodb.com"
        - "mesh.enabled=true"
        - "sidecarContainers[0].name=orchestrator"
        - "sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend:git-${DRONE_COMMIT_SHA:0:7}"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=100m"
        - "resources.requests.memory=256Mi"
        - "sidecarContainers[0].resources.limits.cpu=500m"
        - "sidecarContainers[0].resources.limits.memory=512Mi"
        - "sidecarContainers[0].resources.requests.cpu=100m"
        - "sidecarContainers[0].resources.requests.memory=256Mi"
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
    when:
      event:
        - push

  - name: deploy-planner-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-planner
      values_files: ["environment/staging-planner.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-planner.industrysolutions.staging.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=100m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
    when:
      event:
        - push

  - name: deploy-profile-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-profile
      values_files: ["environment/staging-profile.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-profile.industrysolutions.staging.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=100m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
    when:
      event:
        - push

  - name: deploy-product-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-product
      values_files: ["environment/staging-product.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-product.industrysolutions.staging.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=100m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
    when:
      event:
        - push

---
kind: pipeline
type: kubernetes
name: production

trigger:
  branch:
    - main

steps:
  - name: publish-backend-prod
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/multi-agent-retail-advisor-backend-prod
      dockerfile: backend/Dockerfile
      context: backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  - name: publish-frontend-prod
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/multi-agent-retail-advisor-frontend-prod
      dockerfile: frontend/Dockerfile
      context: frontend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  - name: deploy-app-production
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-prod
      values_files: ["environment/production-app.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-frontend-prod"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor.industrysolutions.prod.corp.mongodb.com"
        - "mesh.enabled=true"
        - "sidecarContainers[0].name=orchestrator"
        - "sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend-prod:git-${DRONE_COMMIT_SHA:0:7}-prod"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=150m"
        - "resources.requests.memory=256Mi"
        - "sidecarContainers[0].resources.limits.cpu=500m"
        - "sidecarContainers[0].resources.limits.memory=512Mi"
        - "sidecarContainers[0].resources.requests.cpu=150m"
        - "sidecarContainers[0].resources.requests.memory=256Mi"
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
    when:
      event:
        - push

  - name: deploy-planner-production
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-planner-prod
      values_files: ["environment/production-planner.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend-prod"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-planner.industrysolutions.prod.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=150m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
    when:
      event:
        - push

  - name: deploy-profile-production
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-profile-prod
      values_files: ["environment/production-profile.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend-prod"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-profile.industrysolutions.prod.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=150m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
    when:
      event:
        - push

  - name: deploy-product-production
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: multi-agent-retail-advisor-product-prod
      values_files: ["environment/production-product.yaml"]
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/multi-agent-retail-advisor-backend-prod"
        - "ingress.enabled=true"
        - "ingress.hosts[0]=multi-agent-retail-advisor-product.industrysolutions.prod.corp.mongodb.com"
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m"
        - "resources.limits.memory=512Mi"
        - "resources.requests.cpu=150m"
        - "resources.requests.memory=256Mi"
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
    when:
      event:
        - push

package com.mongodb.demo.retail.service;

// ADK's Claude model wrapper handles all LLM calls — this class is no longer needed.
// Retained as an empty shell to avoid orphaned Spring bean errors during migration.
public class AnthropicService {}
